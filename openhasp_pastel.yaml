esphome:
  name: esp32-display

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

# Включаем PSRAM
psram:
  mode: octal
  speed: 80MHz

logger:
api:

wifi:
  ssid: "ваш_wi-fi"
  password: "ваш_пароль"

# I2C для сенсора
i2c:
  - id: bus_i2c
    sda: GPIO19
    scl: GPIO20
    frequency: 400kHz

touchscreen:
  - platform: gt911
    id: my_touch
    i2c_id: bus_i2c
    interrupt_pin: GPIO38
    address: 0x5D

# SPI для инициализации дисплея
spi:
  - id: display_spi
    clk_pin: GPIO10
    mosi_pin: GPIO11
    data_rate: 1MHz   # Исправлено: frequency → data_rate

# Настройка дисплея
display:
  - platform: mipi_rgb
    id: my_display
    model: GUITION-4848S040
    rotation: 0
    spi_id: display_spi

# LVGL — создаём надпись "Привет"
lvgl:
  displays:
    - my_display
  widgets:
    - label:
        align: CENTER
        text: "Привет"
        style:
          text_color: 0xFFFFFF
          text_font: montserrat_40

# Подсветка
switch:
  - platform: gpio
    id: backlight
    name: "Подсветка"
    pin: GPIO38
    restore_mode: ALWAYS_ON
